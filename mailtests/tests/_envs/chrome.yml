# `chrome` environment config goes here
modules:
  config:
    WebDriver:
      browser: chrome
      window_size: 1920x1280
      capabilities:
        goog:chromeOptions: # additional chrome options
          args:
            - user-agent=BarCamp Demo