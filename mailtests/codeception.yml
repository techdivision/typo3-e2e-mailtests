params:
    - env
    # activate after configuring the env vars for user and pass
    # - credentials/gmail.env
    # - credentials/yandex.env
paths:
    tests: tests
    output: tests/_output
    data: tests/_data
    support: tests/_support
    envs: tests/_envs
actor_suffix: Tester
extensions:
    enabled:
        - Codeception\Extension\RunFailed
